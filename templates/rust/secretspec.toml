[project]
name = "changeme"
revision = "1.0"

# Declarations only, safe to commit. The profile needs at least one
# secret before enabling secretspec in devenv.yaml.
[profiles.default]
# DATABASE_URL = { description = "Postgres connection string", required = true }

# Other profiles inherit from default and override per environment,
# selected with --profile or SECRETSPEC_PROFILE.
# [profiles.development]
# DATABASE_URL = { required = false, default = "postgres://localhost:5432/dev" }

# [profiles.production]
# SENTRY_DSN = { description = "Error tracking", required = true }
